diff --git a/CMakeLists.txt b/CMakeLists.txt
index d616b0b..d3b643e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -25,27 +25,37 @@ endif()
 
 include(CheckCSourceCompiles)
 
-find_program(YASM_EXE yasm)
-option(ENABLE_NASM "Use nasm if available (Uses yasm by default if found)" OFF)
-if(YASM_EXE AND NOT CMAKE_ASM_NASM_COMPILER MATCHES "yasm" AND NOT ENABLE_NASM)
-    set(CMAKE_ASM_NASM_COMPILER "${YASM_EXE}" CACHE FILEPATH "Path to nasm compatible compiler" FORCE)
+# --- arm64 port: decide once whether this is an x86 target ---
+if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(x86_64|AMD64|amd64|i[3-6]86)$" OR CMAKE_OSX_ARCHITECTURES MATCHES "x86_64")
+    set(SVT_ARCH_X86 TRUE)
 else()
-    set(NASM_VERSION "0.0.0")
-    include(CheckLanguage)
-    check_language(ASM_NASM)
-    execute_process(COMMAND ${CMAKE_ASM_NASM_COMPILER} -v
-        OUTPUT_VARIABLE NASM_VERSION
-        ERROR_QUIET
-        OUTPUT_STRIP_TRAILING_WHITESPACE)
-    string(REGEX MATCH "([.0-9])+" NASM_VERSION "${NASM_VERSION}")
-    # NASM_VERSION should now contain something like 2.14.02
-    # Needs to error out on a version less than 2.14
-    if(NASM_VERSION VERSION_LESS "2.14" AND CMAKE_ASM_NASM_COMPILER MATCHES "nasm")
-        message(FATAL_ERROR "Found nasm is too old (requires at least 2.14, found ${NASM_VERSION})!")
+    set(SVT_ARCH_X86 FALSE)
+    message(STATUS "Non-x86 target (${CMAKE_SYSTEM_PROCESSOR}): building the C fallback paths only")
+endif()
+
+if(SVT_ARCH_X86)
+    find_program(YASM_EXE yasm)
+    option(ENABLE_NASM "Use nasm if available (Uses yasm by default if found)" OFF)
+    if(YASM_EXE AND NOT CMAKE_ASM_NASM_COMPILER MATCHES "yasm" AND NOT ENABLE_NASM)
+        set(CMAKE_ASM_NASM_COMPILER "${YASM_EXE}" CACHE FILEPATH "Path to nasm compatible compiler" FORCE)
+    else()
+        set(NASM_VERSION "0.0.0")
+        include(CheckLanguage)
+        check_language(ASM_NASM)
+        execute_process(COMMAND ${CMAKE_ASM_NASM_COMPILER} -v
+            OUTPUT_VARIABLE NASM_VERSION
+            ERROR_QUIET
+            OUTPUT_STRIP_TRAILING_WHITESPACE)
+        string(REGEX MATCH "([.0-9])+" NASM_VERSION "${NASM_VERSION}")
+        # NASM_VERSION should now contain something like 2.14.02
+        # Needs to error out on a version less than 2.14
+        if(NASM_VERSION VERSION_LESS "2.14" AND CMAKE_ASM_NASM_COMPILER MATCHES "nasm")
+            message(FATAL_ERROR "Found nasm is too old (requires at least 2.14, found ${NASM_VERSION})!")
+        endif()
     endif()
+    enable_language(ASM_NASM)
+    add_definitions(-DARCH_X86_64=1)
 endif()
-enable_language(ASM_NASM)
-add_definitions(-DARCH_X86_64=1)
 
 include(GNUInstallDirs)
 include(CheckCCompilerFlag)
diff --git a/Source/App/SampleDecoder/CMakeLists.txt b/Source/App/SampleDecoder/CMakeLists.txt
index bf5d36f..5943166 100644
--- a/Source/App/SampleDecoder/CMakeLists.txt
+++ b/Source/App/SampleDecoder/CMakeLists.txt
@@ -22,12 +22,7 @@ add_executable(SvtJpegxsSampleDecoder
 #Link only with required libraries to detect in linker time references between decoder_lib an encoder_lib
 set(lib_list_decoder
     $<TARGET_OBJECTS:COMMON_CODEC>
-    $<TARGET_OBJECTS:COMMON_ASM_SSE2>
-    $<TARGET_OBJECTS:COMMON_ASM_AVX2>
     $<TARGET_OBJECTS:DECODER_CODEC>
-    $<TARGET_OBJECTS:DECODER_ASM_AVX512>
-    $<TARGET_OBJECTS:DECODER_ASM_AVX2>
-    $<TARGET_OBJECTS:DECODER_ASM_SSE4_1>
     cpuinfo_public
     )
 
@@ -44,12 +39,7 @@ endif()
 
 add_dependencies(SvtJpegxsSampleDecoder
     COMMON_CODEC
-    COMMON_ASM_SSE2
-    COMMON_ASM_AVX2
     DECODER_CODEC
-    DECODER_ASM_AVX512
-    DECODER_ASM_AVX2
-    DECODER_ASM_SSE4_1
     cpuinfo_public)
 
 install(TARGETS SvtJpegxsSampleDecoder RUNTIME DESTINATION ${CMAKE_INSTALL_FULL_BINDIR})
diff --git a/Source/App/SampleEncoder/CMakeLists.txt b/Source/App/SampleEncoder/CMakeLists.txt
index 4813735..f2a7119 100644
--- a/Source/App/SampleEncoder/CMakeLists.txt
+++ b/Source/App/SampleEncoder/CMakeLists.txt
@@ -22,13 +22,7 @@ add_executable(SvtJpegxsSampleEncoder
 #Link only with required libraries to detect in linker time references between decoder_lib an encoder_lib
 set(lib_list_encoder
     $<TARGET_OBJECTS:COMMON_CODEC>
-    $<TARGET_OBJECTS:COMMON_ASM_SSE2>
-    $<TARGET_OBJECTS:COMMON_ASM_AVX2>
     $<TARGET_OBJECTS:ENCODER_CODEC>
-    $<TARGET_OBJECTS:ENCODER_ASM_SSE2>
-    $<TARGET_OBJECTS:ENCODER_ASM_SSE4_1>
-    $<TARGET_OBJECTS:ENCODER_ASM_AVX2>
-    $<TARGET_OBJECTS:ENCODER_ASM_AVX512>
     cpuinfo_public
     )
 
@@ -45,13 +39,7 @@ endif()
 
 add_dependencies(SvtJpegxsSampleEncoder
     COMMON_CODEC
-    COMMON_ASM_SSE2
-    COMMON_ASM_AVX2
     ENCODER_CODEC
-    ENCODER_ASM_SSE2
-    ENCODER_ASM_SSE4_1
-    ENCODER_ASM_AVX2
-    ENCODER_ASM_AVX512
     cpuinfo_public)
 
 install(TARGETS SvtJpegxsSampleEncoder RUNTIME DESTINATION ${CMAKE_INSTALL_FULL_BINDIR})
diff --git a/Source/Lib/CMakeLists.txt b/Source/Lib/CMakeLists.txt
index a6606b5..6f2c281 100644
--- a/Source/Lib/CMakeLists.txt
+++ b/Source/Lib/CMakeLists.txt
@@ -12,17 +12,19 @@ set(SVT_LIB_VERSION_PATCH 0)
 set(SVT_LIB_VERSION ${SVT_LIB_VERSION_MAJOR}.${SVT_LIB_VERSION_MINOR}.${SVT_LIB_VERSION_PATCH})
 
 add_subdirectory(Common/Codec)
-add_subdirectory(Common/ASM_SSE2)
-add_subdirectory(Common/ASM_AVX2)
 add_subdirectory(Encoder/Codec)
-add_subdirectory(Encoder/ASM_SSE2)
-add_subdirectory(Encoder/ASM_SSE4_1)
-add_subdirectory(Encoder/ASM_AVX2)
-add_subdirectory(Encoder/ASM_AVX512)
 add_subdirectory(Decoder/Codec)
-add_subdirectory(Decoder/ASM_SSE4_1)
-add_subdirectory(Decoder/ASM_AVX2)
-add_subdirectory(Decoder/ASM_AVX512)
+if(SVT_ARCH_X86)
+    add_subdirectory(Common/ASM_SSE2)
+    add_subdirectory(Common/ASM_AVX2)
+    add_subdirectory(Encoder/ASM_SSE2)
+    add_subdirectory(Encoder/ASM_SSE4_1)
+    add_subdirectory(Encoder/ASM_AVX2)
+    add_subdirectory(Encoder/ASM_AVX512)
+    add_subdirectory(Decoder/ASM_SSE4_1)
+    add_subdirectory(Decoder/ASM_AVX2)
+    add_subdirectory(Decoder/ASM_AVX512)
+endif()
 
 if(UNIX)
     if(NOT APPLE)
@@ -58,19 +60,25 @@ if (BUILD_SHARED_LIBS)
 endif()
 
 # Decoder Lib Source Files
+set(SVT_ASM_OBJECTS "")
+if(SVT_ARCH_X86)
+    set(SVT_ASM_OBJECTS
+        $<TARGET_OBJECTS:COMMON_ASM_AVX2>
+        $<TARGET_OBJECTS:COMMON_ASM_SSE2>
+        $<TARGET_OBJECTS:ENCODER_ASM_SSE2>
+        $<TARGET_OBJECTS:ENCODER_ASM_SSE4_1>
+        $<TARGET_OBJECTS:ENCODER_ASM_AVX2>
+        $<TARGET_OBJECTS:ENCODER_ASM_AVX512>
+        $<TARGET_OBJECTS:DECODER_ASM_AVX512>
+        $<TARGET_OBJECTS:DECODER_ASM_AVX2>
+        $<TARGET_OBJECTS:DECODER_ASM_SSE4_1>)
+endif()
+
 add_library(SvtJpegxsLib
     $<TARGET_OBJECTS:COMMON_CODEC>
-    $<TARGET_OBJECTS:COMMON_ASM_AVX2>
-    $<TARGET_OBJECTS:COMMON_ASM_SSE2>
     $<TARGET_OBJECTS:ENCODER_CODEC>
-    $<TARGET_OBJECTS:ENCODER_ASM_SSE2>
-    $<TARGET_OBJECTS:ENCODER_ASM_SSE4_1>
-    $<TARGET_OBJECTS:ENCODER_ASM_AVX2>
-    $<TARGET_OBJECTS:ENCODER_ASM_AVX512>
     $<TARGET_OBJECTS:DECODER_CODEC>
-    $<TARGET_OBJECTS:DECODER_ASM_AVX512>
-    $<TARGET_OBJECTS:DECODER_ASM_AVX2>
-    $<TARGET_OBJECTS:DECODER_ASM_SSE4_1>
+    ${SVT_ASM_OBJECTS}
     )
 
 set_target_properties(SvtJpegxsLib PROPERTIES OUTPUT_NAME "SvtJpegxs")
diff --git a/Source/Lib/Common/Codec/common_dsp_rtcd.c b/Source/Lib/Common/Codec/common_dsp_rtcd.c
index 88817ab..3630f98 100644
--- a/Source/Lib/Common/Codec/common_dsp_rtcd.c
+++ b/Source/Lib/Common/Codec/common_dsp_rtcd.c
@@ -43,6 +43,11 @@ CPU_FLAGS get_cpu_flags() {
 
     return flags;
 }
+#else  /* !ARCH_X86_64 */
+CPU_FLAGS get_cpu_flags() {
+    /* No x86 SIMD on this target: every dispatch falls through to the C path. */
+    return 0;
+}
 #endif /*ARCH_X86_64*/
 
 #ifdef ARCH_X86_64
diff --git a/Source/Lib/Common/Codec/common_dsp_rtcd.h b/Source/Lib/Common/Codec/common_dsp_rtcd.h
index 3fdc957..3d47385 100644
--- a/Source/Lib/Common/Codec/common_dsp_rtcd.h
+++ b/Source/Lib/Common/Codec/common_dsp_rtcd.h
@@ -28,9 +28,9 @@ extern "C" {
 #endif
 
 // Helper Functions
-#ifdef ARCH_X86_64
+/* arm64 port: callers (EncHandle/DecHandle) invoke this unconditionally, so it is
+   declared on every arch; the non-x86 definition simply reports no SIMD. */
 CPU_FLAGS get_cpu_flags();
-#endif
 void setup_common_rtcd_internal(CPU_FLAGS flags);
 uint32_t log2_32_c(uint32_t x);
 RTCD_EXTERN uint32_t (*svt_log2_32)(uint32_t x);
diff --git a/Source/Lib/Decoder/Codec/decoder_dsp_rtcd.c b/Source/Lib/Decoder/Codec/decoder_dsp_rtcd.c
index 3318140..b266e04 100644
--- a/Source/Lib/Decoder/Codec/decoder_dsp_rtcd.c
+++ b/Source/Lib/Decoder/Codec/decoder_dsp_rtcd.c
@@ -5,19 +5,21 @@
 
 #define DECODER_RTCD_C
 #include "decoder_dsp_rtcd.h"
-#include "Dwt53Decoder_AVX2.h"
 #include "Dequant.h"
-#include "Dequant_SSE4.h"
 #include "Idwt.h"
 #include "NltDec.h"
-#include "NltDec_AVX2.h"
 #include "Precinct.h"
+#include "Packing.h"
+#ifdef ARCH_X86_64
+#include "Dwt53Decoder_AVX2.h"
+#include "Dequant_SSE4.h"
+#include "NltDec_AVX2.h"
 #include "UnPack_avx2.h"
 #include "UnPack_avx512.h"
-#include "Packing.h"
 #include "idwt-avx512.h"
 #include "NltDec_avx512.h"
 #include "Dequant_avx512.h"
+#endif /* ARCH_X86_64 */
 
 /**************************************
  * Instruction Set Support
diff --git a/Source/Lib/Encoder/Codec/encoder_dsp_rtcd.c b/Source/Lib/Encoder/Codec/encoder_dsp_rtcd.c
index 365b4ee..b6f4676 100644
--- a/Source/Lib/Encoder/Codec/encoder_dsp_rtcd.c
+++ b/Source/Lib/Encoder/Codec/encoder_dsp_rtcd.c
@@ -6,23 +6,25 @@
 #define ENCODER_RTCD_C
 #include "encoder_dsp_rtcd.h"
 #include "GcStageProcess.h"
-#include "NltEnc_avx2.h"
 #include "DwtStageProcess.h"
-#include "Enc_avx512.h"
 #include "GcStageProcess.h"
 #include "Dwt.h"
-#include "Dwt_AVX2.h"
 #include "NltEnc.h"
+#include "Quant.h"
+#include "PackPrecinct.h"
+#include "RateControl.h"
+#ifdef ARCH_X86_64
+#include "NltEnc_avx2.h"
+#include "Enc_avx512.h"
+#include "Dwt_AVX2.h"
 #include "Quant_sse4_1.h"
 #include "Quant_avx2.h"
 #include "Quant_avx512.h"
-#include "Quant.h"
-#include "PackPrecinct.h"
 #include "Pack_avx512.h"
 #include "Pack_avx2.h"
 #include "group_coding_sse4_1.h"
-#include "RateControl.h"
 #include "RateControl_avx2.h"
+#endif /* ARCH_X86_64 */
 
 /**************************************
  * Instruction Set Support
